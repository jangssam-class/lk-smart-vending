# V1.9 포스팅 CMS + 네이버 검색수집 구조

## 포스팅 흐름
1. 사이트 배포 후 `/admin/` 접속
2. 관리자 로그인
3. `지역 마케팅 포스팅` → 새 포스팅
4. 제목, 설명문, 지역, 제품, 이미지, 본문 작성
5. `Publish` 클릭
6. Decap CMS가 GitHub 저장소에 콘텐츠 커밋
7. Netlify가 자동으로 새 배포 시작
8. Eleventy가 정적 HTML 포스팅 페이지를 생성
9. `/posts/`, `/sitemap.xml`, `/rss.xml`이 자동 갱신

## 네이버 노출을 위해 최초 1회 필요한 작업
1. Netlify 실제 사이트 주소 확정
2. `_data/site.json`의 `url`을 실제 주소로 변경
3. GitHub에 업로드하고 Netlify 배포
4. 네이버 서치어드바이저에 새 사이트 등록 및 소유확인
5. `sitemap.xml` 제출
6. 첫 글 발행 후 `rss.xml` 제출
7. 중요한 신규 글은 필요할 때 웹페이지 수집 요청

## CMS 활성화(Netlify)
- Netlify에서 새 `lk-smart-vending` 저장소를 연결
- Integrations → Identity → Netlify Identity 활성화
- Registration은 Invite only 권장
- Git Gateway 활성화
- 관리자 계정을 초대
- `/admin/`에서 로그인

## 중요
검색로봇이 글을 수집·색인할 수 있게 만드는 시스템입니다.
발행 즉시 검색 노출 또는 특정 키워드 상위노출을 보장하는 기능은 아닙니다.
